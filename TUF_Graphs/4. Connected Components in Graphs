Connected Components in Graphs

components components components


for(int i=0; i<10; i++)
    if(!vis[i])
        dfs
        bfs
        
How to find the number of components?
Count the number of times the code goes inside the if statement.
